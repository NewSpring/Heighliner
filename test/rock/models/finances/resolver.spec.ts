import test from "ava";
// import Resolver from "../../../../src/rock/models/campuses/resolver";

// const sampleData = {
//   transaction: {
//     Id: "123ABC",
//     Amount: "25.00",
//     AccountId: "12345",
//   },
// };

// test("`TransactionDetail` has amount and account.", t => {
//   t.pass();
// });

// test("`ScheduledTransaction` has start and end date.", t => {
//   t.pass();
// });

// test("`ScheduledTransaction` has required meta data.", t => {
//   t.pass();
// });

// test("`Transaction` has payment details.", t => {
//   t.pass();
// });

// test("`FinancialAccount` is a valid account for contributions.", t => {
//   t.pass();
// });

// test("`PaymentDetail` has detailed information about a single payment.", t => {
//   t.pass();
// });

// test("`SavedPayment` has the required information to repeat transactions.", t => {
//   t.pass();
// });

test.plan("`TransactionDetail` has amount and account.");
test.plan("`ScheduledTransaction` has start and end date.");
test.plan("`ScheduledTransaction` has required meta data.");
test.plan("`Transaction` has payment details.");
test.plan("`FinancialAccount` is a valid account for contributions.");
test.plan("`PaymentDetail` has detailed information about a single payment.");
test.plan("`SavedPayment` has the required information to repeat transactions.");
